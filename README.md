# restorable CLI

This is the command-line interface (CLI) for `restorable`, a tool designed to provide automated database restore verification with signed, timestamped verification reports, alerts when restores fail or stop running and an audit trail, suitable for security reviews. 

## Installation
tbd
